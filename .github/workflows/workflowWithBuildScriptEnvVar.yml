on: [push]
env:
    NAMESPACE: jysindemo-ns2
    REGISTRY_URL_1: jysintestregistry.azurecr.io
    1_REGISTRY_USERNAME: ${{ secrets.AZURE_CLIENT_ID }}
    1_REGISTRY_PASSWORD: ${{ secrets.AZURE_CLIENT_SECRET }}
    1_IMAGE_NAME: jysintestregistry.azurecr.io/jysindemoimage:first
    1_IMAGE_DOCKERFILE:  ./Dockerfile

  
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
      
    - uses: azure/k8s-set-context@v1
      with:
         kubeconfig: ${{ secrets.AKS_JYSINAKS2_KUBECONFIG }}
      id: login
    
    - name: Create namespace actions
      run: |
        namespacePresent=`kubectl get namespace | grep $NAMESPACE | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace $NAMESPACE`
        fi
    
    - uses: Azure/k8s-create-secret@v1
      with:
        namespace: ${{ env.NAMESPACE  }}
        container-registry-url: ${{ env.1_REGISTRY_URL }}
        container-registry-username: ${{ env.1_REGISTRY_USERNAME }}
        container-registry-password: ${{ env.1_REGISTRY_PASSWORD }}
        secret-name: demo-k8s-secret
            
    - uses: Azure/k8s-deploy@users/jysin/ConfigFilePathsAddition1_3
      with:
        namespace: ${{ env.NAMESPACE  }}
        imagepullsecrets: demo-k8s-secret
        manifests: |
          .devcontainer/manifests/deployment.yml
          .devcontainer/manifests/service.yml
        images: |
          jysintestregistry.azurecr.io/jysindemoimage:first
